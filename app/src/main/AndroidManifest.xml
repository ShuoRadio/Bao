<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.BuyMenuActivity"></activity>
        <activity android:name=".activity.MainActivity"></activity>
        <activity android:name=".activity.User_LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.User_RegisterActivity"></activity>
        <activity android:name=".activity.Restrant_LoginActivity" />
        <activity android:name=".activity.Restrant_RegisterActivity" />
        <activity android:name=".activity.ModifyInfoActivity" />
        <activity android:name=".activity.OrderDetailedActivity" />
        <activity android:name=".activity.OrderOkActivity"/>
    </application>

</manifest>